# v0.1.22-hotfix3 Post-review Evidence

Date: 2026-09-01. Baseline: `f3d3ba2e2b2683c9a5478c86e73fafecef8b209c`.

- PDF root exception: `Barryvdh\DomPDF\Facade\Pdf` was referenced but `barryvdh/laravel-dompdf` is not installed; direct `dompdf/dompdf` 3.1.6 is installed. The fixed path uses `Dompdf\Dompdf`, local Cairo TTFs, writable font cache, PDF-only PNG barcodes, and explicit thermal/A4 geometry.
- Focused Hotfix3 correction pass: 9 tests, 143 assertions, 9 passed, zero failures. The two obsolete assertions were replaced with behavior-aligned contracts for company-scoped active parent/child selection and the canonical pricing-label route/service/view without weakening the remaining requirement-family coverage.
- Render primitives: Code 128 `TESTLOCAL0001` and EAN-13 `4006381333931` produced valid PNG signatures. Quantity examples rendered `2`, `-2`, `0`; `1.5` raised an explicit integrity exception.
- Real authenticated requests against disposable socket-only MariaDB database `rajeh_hotfix3_verify` produced three HTTP 200 `application/pdf` downloads with `%PDF` signatures and safe localized `ملصقات-الباركود.pdf` filenames. The 50×30 mm output produced exactly three Code 128 pages for `TESTLOCAL0001` and exactly one EAN-13 page for `4006381333931`; the configured A4 template produced one page containing one EAN-13 label. Every page was rasterized at 300 DPI and decoded from the raster with ZXing; exact copy counts and values passed. Cairo Arabic, product name, item code, barcode value, and price were visually present without clipping.
- Authenticated Arabic desktop/mobile evidence covered balances, transfers, counts, movement register, customer create/edit/groups, expanded desktop sidebar, and the mobile drawer. Route-specific content/headings were distinct, tables aligned, quantities were whole, movement labels localized, five isolated company groups (parents and children) appeared, the saved child group survived reload, the foreign-company group stayed hidden, navigation/sidebar requirements passed, and there were zero console errors or document overflow.
- PHP syntax, compiled Blade, Arabic/English JSON parsing, isolated migration status (97 migrations ran), and diff hygiene passed once. No additional Vite build ran because no compiled frontend asset changed after the previously passed build.
- Production/shared data, database, deployment, active symlink, tags and remotes were not accessed or mutated.
