# v0.1.22-hotfix34 Focused Verification

- Baseline: exact Hotfix32 commit `2636d8dc1d345fa050e4e20c729a4cab9ac4a35a` in a separate clean `rajeh_ahmed` worktree; accepted Hotfix33 commit history was reapplied without modifying its release or artifacts.
- Root cause: Hotfix33's runtime verifier directly invoked the cached `locale.switch` route action. After `route:cache`, that action depended on Laravel Serializable Closure restoration and failed before the switch. Hotfix34 dispatches through `App\Http\Controllers\LocaleController` and tests the real HTTP endpoint through the cached application kernel.
- Controller contract: POST `/locale`, route name `locale.switch`, web middleware/CSRF, redirect-back behavior, session persistence, and one-year `locale` cookie are preserved. Validation uses the controller's exact `['ar', 'en', 'ar-EG']` allowlist; malformed and unsupported values leave the preference unchanged.
- Focused packaged verification covers touched PHP syntax, cached route creation and boot, real three-locale switching and public HTML, fallback/RTL/Cairo/sidebar/command palette, 6,115 JSON and 244 PHP leaf parity, protected tokens, unchanged `ar`/`en`, Hotfix32 invoice-search/keyboard compiled runtime, exact asset identity, archive/manifest integrity, no migration, Git diff hygiene, Bash syntax, and isolated pre/post-switch forced failures.
- Activation-gated: read-only authenticated HTTP 200 rendering for dashboard, products, purchase orders, purchase invoices, pricing, POS, and reports in `ar`, `en`, and `ar-EG`, plus real invoice-search responses, executes inside a transaction before and after switching; every transaction is rolled back.
- Vite provenance: build count zero. Exact Hotfix33 compiled assets are reused; manifest SHA-256 is `531fe85dc096655440dc21bc2213eee85ca3c6062cb16c79327d8fce729a0182`.
- Migration status: Hotfix34 adds and executes no migration. Migration 000111 must already be `Ran`; activation executes no migration.
