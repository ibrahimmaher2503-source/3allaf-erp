# v0.1.22-hotfix33 Focused Verification

- Baseline: exact Hotfix32 commit `2636d8dc1d345fa050e4e20c729a4cab9ac4a35a` in a separate clean `rajeh_ahmed` worktree.
- Localization source: commit `be300bfb039316a3f89b29f2cd2c29bd73bd0add`; expected tracking conflicts preserved both histories. The POS customer-search locale-dependent `ORDER BY` hunk was rejected; all accepted `app/` and `routes/` changes are locale registration, validation/switching, persistence/fallback, Arabic-family presentation, RTL, or typography only.
- PASS: touched PHP syntax; 6,115 JSON keys and 244 PHP leaf keys with exact parity and protected-token parity; unchanged `ar`/`en`; locale switch/session/cookie/fallback; exact `lang="ar-EG"`, RTL, Cairo, sidebar, command palette; Hotfix32 invoice-search/keyboard compiled runtime; compiled asset integrity; 1,515-location audit count/path; no migration; Git diff hygiene; Bash syntax and isolated fail-closed activation behavior.
- The focused runtime probe found that Laravel `Application::isLocale()` does not accept wildcard patterns. The source commit's 532 `isLocale('ar*')` calls across 107 files were therefore replaced mechanically with an actual Arabic-family prefix test; this prevents both existing `ar` and new `ar-EG` from falling into English presentation. No action, calculation, authorization, query, posting, or schema behavior changed.
- Vite provenance: Node 20.20.0, npm 10.8.2, Vite 8.2.0. Two Node 20 builds completed in the integration worktree; the second was the single final build after the Arabic-family correction and produced manifest SHA-256 `531fe85dc096655440dc21bc2213eee85ca3c6062cb16c79327d8fce729a0182`. One earlier mis-launched npm command resolved system Node 16 and failed before asset generation.
- Activation-gated: authenticated HTTP 200 and correct locale rendering for dashboard, products, purchase orders, purchase invoices, pricing, POS, and reports in `ar`, `en`, and `ar-EG`, plus Egyptian switching/persistence/fallback and invoice search/keyboard checks, run both before and after switching.
- Migration status: Hotfix33 adds and executes no migration. Migration 000111 must already be `Ran`; activation executes no migration.
